"""LumaStage AI core package.

The modules in this package deliberately keep the API and persistence layers
independent from Cinema 4D so they can be tested with a normal Python runtime.
"""

__version__ = "2.0.7"
